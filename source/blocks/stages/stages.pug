include ../header-of-block/header-of-block.pug
include ../stage/stage.pug

mixin stages(options = {header: "stages", stages: {}})
    -let {header = "stages", stages = {}} = options;
    section.stages
        +header-of-block(header)
        .stages__graphics
            +stage(stages)